<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!--<item name="android:buttonStyle">@style/CustomButtonStyle</item>-->
    </style>

    <style name="CustomButtonStyle" parent="@style/Widget.AppCompat.Button">
        <item name="android:background">@color/colorButtonBackground</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <!--@style/Widget.AppCompat.Button   android:style/Widget.Holo.Button  @style/Base.Widget.AppCompat.Button-->
</resources>
